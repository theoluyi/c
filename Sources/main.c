#include <stdio.h>
#include <stdlib.h>
// need these instructions

// main is a method
// main will get executed when we run our program

// build/compile c file is first step
// then have to run it
int main()
{
  int age = 40;
  double gpa = 3.7;
  char grade = "A";
  char phrase[] = "Goofballs in space";

  return 0;
}

